{
  "version": "1.79.0",
  "notes": "The app tiles all shrank to the same small icon-and-name chip, so the whole launcher fits on a phone screen instead of scrolling. The Controls buttons match them now too — one tidy grid instead of two sets of buttons at different sizes. The short description under each app moved to the tooltip you get by hovering, so the Files & Printer and Slideshow status lines are no longer on the tile itself.",
  "url": "https://github.com/Stevo238/home-hub-releases/releases/download/v1.79.0/HomeHub.exe"
}
