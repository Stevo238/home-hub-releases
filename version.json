{
  "version": "1.71.0",
  "notes": "Files: folders now show their real size (everything inside them) instead of an item count, with the file count on hover, so the root view and the totals line reflect the whole share. Sorting by size works on folders too. Also from 1.70: sort by name, date, size or type via the column headers or the sort menu, and a ⓘ details panel on every row with kind, exact size, photo dimensions, created/modified dates and folder contents.",
  "url": "https://github.com/Stevo238/home-hub-releases/releases/download/v1.71.0/HomeHub.exe"
}
